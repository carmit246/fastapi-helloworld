name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

env:
  AWS_REGION: eu-west-1
  EKS_CLUSTER_NAME: test1-eks
  IMAGE_NAME: test1-app

jobs:
  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python with caching
        uses: ./.github/actions/setup-python
        with:
          python-version: '3.8'
          cache-key-suffix: 'lint'

      - name: Install lint dependencies
        run: pip install darker[isort]

      - name: Run darker
        run: darker --check --diff --isort app/ tests/

  audit:
    name: Security Audit
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python with caching
        uses: ./.github/actions/setup-python
        with:
          python-version: '3.8'
          cache-key-suffix: 'audit'

      - name: Install dependencies
        run: pip install pip-audit

      - name: Run pip-audit
        continue-on-error: true
        run: pip-audit --desc

  test:
    name: Test Application
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Python with caching
        uses: ./.github/actions/setup-python
        with:
          python-version: '3.8'
          cache-key-suffix: 'test'
          requirements-file: true

      - name: Install dependencies
        run: |
          pip install -r requirements.txt
          pip install pytest

      - name: Run tests
        run: pytest tests/ -v

  build-and-push:
    name: Build and Push to ECR
    environment: dev
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    needs: [lint, audit, test]
    permissions:
      contents: read
      packages: write
    
    outputs:
      image-uri: ${{ steps.build-image.outputs.image }}
      regitry-uri: ${{ steps.login-ecr.outputs.registry }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Build, tag, and push image to Amazon ECR
        id: build-image
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
          ECR_REPOSITORY: ${{ env.IMAGE_NAME }}
        run: |
          echo "Building Docker image..."
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .          
          echo "Pushing image to ECR..."
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
          echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> $GITHUB_OUTPUT
          echo "✅ **Image pushed: $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" >> "$GITHUB_STEP_SUMMARY"

  deploy:
    needs: build-and-push
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'    
    uses: carmit246/fastapi-helloworld/.github/workflows/deploy-backend.yaml@main
    secrets: inherit
    with:
      env: dev
      repository-uri: ${{ needs.build-and-push.outputs.regitry-uri }}
      image-tag: ${{ github.sha }}